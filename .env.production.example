# ShimmyServeAI Production Environment Configuration

# ==========================================
# APPLICATION SETTINGS
# ==========================================
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# Frontend URL (update with your domain)
FRONTEND_URL=https://your-domain.com
CORS_ORIGIN=https://your-domain.com

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
DATABASE_PATH=/app/data/shimmy.db
BACKUP_PATH=/app/data/backups

# ==========================================
# SECURITY CONFIGURATION
# ==========================================
# IMPORTANT: Change these in production!
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_64_CHARACTERS_LONG
BCRYPT_ROUNDS=12
SESSION_SECRET=CHANGE_THIS_TO_ANOTHER_SECURE_RANDOM_STRING_FOR_SESSIONS

# SSL Configuration (if using HTTPS)
FORCE_HTTPS=true
SSL_CERT_PATH=/app/ssl/cert.pem
SSL_KEY_PATH=/app/ssl/key.pem

# ==========================================
# RATE LIMITING
# ==========================================
RATE_LIMIT_WINDOW_MS=900000    # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS=100    # Max requests per window
AUTH_RATE_LIMIT_MAX=5          # Max auth attempts per window

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
LOG_LEVEL=info
LOG_FILE_PATH=/app/data/logs/app.log
LOG_MAX_SIZE=10mb
LOG_MAX_FILES=10
ENABLE_ACCESS_LOGS=true

# ==========================================
# MONITORING & HEALTH CHECKS
# ==========================================
METRICS_ENABLED=true
HEALTH_CHECK_PATH=/api/health
HEALTH_CHECK_INTERVAL=30000    # 30 seconds

# ==========================================
# PERFORMANCE SETTINGS
# ==========================================
# Node.js memory settings
NODE_OPTIONS="--max-old-space-size=1024"

# Database optimization
DB_CONNECTION_POOL_SIZE=10
DB_QUERY_TIMEOUT=30000

# ==========================================
# FEATURE FLAGS
# ==========================================
ENABLE_REGISTRATION=true
ENABLE_PASSWORD_RESET=true
ENABLE_USER_MANAGEMENT=true
ENABLE_SYSTEM_MONITORING=true
ENABLE_TERMINAL_ACCESS=true

# ==========================================
# BACKUP CONFIGURATION
# ==========================================
BACKUP_ENABLED=true
BACKUP_INTERVAL=86400000       # Daily backups (24 hours in milliseconds)
BACKUP_RETENTION_DAYS=30       # Keep backups for 30 days

# ==========================================
# EXTERNAL SERVICES (Optional)
# ==========================================
# Email service (for notifications)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM=noreply@your-domain.com

# Analytics (if using)
# ANALYTICS_ENABLED=false
# ANALYTICS_KEY=your-analytics-key

# ==========================================
# DEVELOPMENT OVERRIDES
# ==========================================
# These should be false in production
ENABLE_DEBUG_ROUTES=false
ENABLE_MOCK_DATA=false
ENABLE_VERBOSE_LOGGING=false

# ==========================================
# CONTAINER SPECIFIC
# ==========================================
# These are set by Docker, but can be overridden
# HOSTNAME=shimmyserveai-prod
# TZ=UTC